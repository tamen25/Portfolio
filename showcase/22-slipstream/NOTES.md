# 22 SLIPSTREAM — build notes

## Concept
Jet-age air line. Signature: a 260vh sticky scroll morph — a hand-authored SVG
engineering drawing (cyanotype grid, stroke-dashoffset line draw, gold dimension
lines: "DO NOT SCALE FROM DRAWING") that becomes the generated photograph via a
clip-path wipe with a gold scan line; captions advance DRAWN → DIMENSIONED →
BECOMING → AIRBORNE. Route board + creed carry the era's voice.

## Assets
- soul_2 liner in golden clouds; model delivered a JET-age design with gibberish
  fuselage lettering → nano_banana_pro edit removed lettering; the site's fiction
  shifted 1936→1958 to match the machine (cheaper than fighting the model, and the
  blueprint's lines suit a jet). Blueprint prop swapped for an engine pod.
- ~1.5 credits total. liner.webp 97KB.

## Type / palette
Poiret One (deco display) + Public Sans. Cyanotype #16324F + drafting white + gold #D9A441.

## Iteration log
- PASS 1: line-draw + dimension fade verified on the blueprint phase.
- PASS 2: asset hygiene (lettering edit) + era coherence rewrite (copy, routes, alt,
  blueprint engine) — the fiction now matches the pixels.
- PASS 3: mid-morph verified (wipe + scan line + linework ahead of the reveal);
  reduced motion = everything pre-drawn, photo full. Mobile: stage scales, route
  table collapses to stacked rows.
- STATUS: BUILT + QA'd locally. DEPLOY BLOCKED (Netlify credits) — ship when unblocked.
