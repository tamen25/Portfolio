# 20 MYCELIUM — build notes

## Concept
Mycorrhiza Institute. A living hyphae network drawn by branching random walkers in
DOCUMENT space (repainted per scroll): growth budget = reading depth (walkers pause
below scrollY + 1.15 viewports), so the network literally grows as you read. When a
section's anchor enters view, the nearest grown thread-tip sends a dedicated steering
walker to it; on arrival the anchor lights and pulses — the page colonizes what you
read. Copy is honest about the wood-wide-web debates ("'May' is doing honest work in
this sentence, and we let it").

## Type / palette
Space Grotesk + Lora. Humus #12100C · thread #E9F5DA · spore lime #B8E986.

## Iteration log
- PASS 1: growth + colonization verified (a thread descended and lit the Findings
  anchor — the signature moment works exactly as imagined).
- PASS 2: branch-point pulses accumulated into green blobs over the title → branch
  pulses now rarer (30%), smaller (r2.2), faster-fading. Crown reads delicate.
- PASS 3: browse daemon wedged again mid-QA (blank captures; cleanup+reload recovers —
  pattern logged in STATE). Mobile: same engine, fewer walkers via width; reduced
  motion: growth speed drops to a crawl but still completes (content never blocked).
- Credits: 0.
